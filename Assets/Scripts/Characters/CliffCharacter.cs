using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CliffCharacter : BaseCharacter
{
    // Start is called before the first frame update
    void Start()
    {
        isCliff = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
